{% extends "base.html.twig" %}

{% block title %}
Edition de {{ category.name }}
{% endblock %}

{% block body %}
<h1>edition </h1>

{{ form_start(formView) }}

{{ form_errors(formView)}}


{{ form_row(formView.name) }}


<button type="submit" class="btn btn-primary mt-3">
    <i class="fas fa-save"> Créer le catégorie</i>
</button>

{{ form_end(formView) }}
{% endblock %}